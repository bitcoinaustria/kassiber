{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window can call the Kassiber daemon command boundary.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
