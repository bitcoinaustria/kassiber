{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window can call the Kassiber daemon command boundary and listen to streamed daemon events.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
