/**
 * Transactions placeholder.
 *
 * Real translation of claude-design/screens/transactions.jsx is the
 * next screen target. The route is defined now so AppHeader nav links
 * resolve.
 */

export function Transactions() {
  return (
    <div className="flex-1 p-12 font-sans text-ink">
      <div className="kb-mono-caption mb-2">transactions · placeholder</div>
      <h1 className="m-0 text-3xl font-semibold tracking-tight">
        Transactions
      </h1>
      <p className="mt-2 text-sm text-ink-2">
        TanStack Table virtualization + cursor pagination lands when this
        screen translates.
      </p>
    </div>
  );
}
