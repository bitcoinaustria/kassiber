[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kassiber"
version = "0.21.0"
description = "Open-source, local-first Bitcoin accounting CLI with multi-account and multi-wallet support."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "AGPL-3.0-only" }
authors = [
  { name = "Bitcoin Austria" }
]
dependencies = [
  "embit>=0.8.0",
  "PySide6>=6.7,<7",
  "rp2 @ git+https://github.com/bitcoinaustria/rp2.git@2fc8592cd952f7aa64669a8ffbd1476dfc48abf9",
]

[project.scripts]
kassiber = "kassiber.cli.main:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["kassiber*"]

[tool.setuptools.package-data]
"kassiber.ui" = [
  "resources/qml/*.qml",
  "resources/qml/components/*.qml",
  "resources/qml/dialogs/*.qml",
]
